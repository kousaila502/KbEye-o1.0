# KbEye Project Environment Configuration
# Copy this file to .env and customize for your setup

# =============================================================================
# PROJECT INFORMATION
# =============================================================================
PROJECT_NAME=KbEye
PROJECT_VERSION=1.0.0
ENVIRONMENT=development

# =============================================================================
# NETWORK CONFIGURATION
# =============================================================================

# Backend Configuration
BACKEND_HOST=localhost
BACKEND_PORT=8000
BACKEND_PROTOCOL=http

# Frontend Configuration  
FRONTEND_HOST=localhost
FRONTEND_PORT=5173
FRONTEND_PROTOCOL=http

# WebSocket Configuration
WEBSOCKET_PROTOCOL=ws

# =============================================================================
# CONSTRUCTED URLS (DO NOT EDIT - AUTO-GENERATED)
# =============================================================================
# These are automatically constructed from the above values
# Backend API URL: ${BACKEND_PROTOCOL}://${BACKEND_HOST}:${BACKEND_PORT}
# Frontend URL: ${FRONTEND_PROTOCOL}://${FRONTEND_HOST}:${FRONTEND_PORT}  
# WebSocket URL: ${WEBSOCKET_PROTOCOL}://${BACKEND_HOST}:${BACKEND_PORT}

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=kbeye
DB_USER=postgres
DB_PASSWORD=your_password_here
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# =============================================================================
# DEPLOYMENT SETTINGS
# =============================================================================

# CORS Origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Security
DEBUG_MODE=true
LOG_LEVEL=info

# =============================================================================
# DOCKER CONFIGURATION
# =============================================================================
COMPOSE_PROJECT_NAME=kbeye
RESTART_POLICY=unless-stopped

# =============================================================================
# PRODUCTION OVERRIDES (uncomment for production deployment)
# =============================================================================

# For Production Deployment:
# ENVIRONMENT=production
# BACKEND_HOST=api.yourdomain.com
# BACKEND_PROTOCOL=https
# FRONTEND_HOST=yourdomain.com  
# FRONTEND_PROTOCOL=https
# WEBSOCKET_PROTOCOL=wss
# DEBUG_MODE=false
# LOG_LEVEL=warning

# For Docker Production:
# BACKEND_HOST=kbeye-backend
# DB_HOST=kbeye-db
# ALLOWED_ORIGINS=https://yourdomain.com